import { SettingsView } from '@/components/settings/settings-view';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Settings - Helpa',
};

export default function SettingsPage() {
  return <SettingsView />;
}
