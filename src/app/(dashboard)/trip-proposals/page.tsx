'use client';

import TripProposalsPage from '../quotations/page';

/**
 * Canonical travel-facing route for the existing quotation workflow.
 * The underlying implementation remains shared so no CRM functionality is lost.
 */
export default function Page() {
  return <TripProposalsPage />;
}
