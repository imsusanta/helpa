/**
 * Helpa Core Platform — Industry Module Contract
 *
 * Core-owned industry vocabulary (terminology, aliases) and the port through
 * which platform layers consume industry manifests without depending on the
 * `src/modules` layer.
 */

export * from './terminology';
export * from './industry-port';